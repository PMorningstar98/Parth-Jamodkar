export const metadata = {
  title: 'Public Work',
  description:
    'Every Microsoft publication, conference talk, GitHub repository, merged pull request, and blog post by Parth Jamodkar — aggregated in one index.',
};

export default function PublicWorkLayout({ children }) {
  return children;
}
